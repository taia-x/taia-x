name: Tests

on: [push, pull_request]

jobs:
  test-provider:
    name: Run tests

    runs-on: ubuntu-18.04

    strategy:
      matrix:
        python-version: []

    steps:
      - uses: actions/checkout@master

      - name: Set up Python
        uses: actions/setup-python@v2
          with:
            python-version: 3.9.7
        
      - name: Install dependencies
        run: cd provider & pip install -r requirements.txt

      - name: Run tests
        run: cd provider & pytest