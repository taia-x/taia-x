version: "3.4"

services:
  provider:
    container_name: provider-node
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./:/backend/
    entrypoint: ./docker-start.sh
    env_file:
      - ./.env
    ports:
      - 5678:5678
      - 8000:8000

networks:
  taia-x:
    name: taia-x
