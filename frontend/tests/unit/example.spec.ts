// import { shallowMount } from "@vue/test-utils";

describe("Vue can add ", () => {
  it("calculates 1 + 1", () => {
    // const msg = "TAIA-X";
    // const wrapper = shallowMount(defineComponent, {
    //   props: { msg },
    // });
    // expect(wrapper.text()).toMatch(msg);
    expect(1 + 1).toEqual(2);
  });
});
